MUHAMMED_PASSWORD=choose-a-strong-password
AYAAN_PASSWORD=choose-a-strong-password

# Set these only when using a persistent host volume, such as a paid Render disk.
# DATA_DIR=/var/data
# UPLOADS_DIR=/var/data/uploads
